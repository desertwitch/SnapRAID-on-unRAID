# SnapRAID configuration file (symlinked to /etc/snapraid.conf)
# https://www.snapraid.it/manual
# Use trailing slashes (/mnt/disk1->/mnt/disk1/) where possible


# Recommended excludes:
# Unrecoverables and often changing paths (System, Docker, VMs)
exclude *.unrecoverable
exclude /system/
exclude /appdata/
exclude /domains/

# Recommended auto-save during SYNC operations (in GB):
# In case of interruptions no need to start from scratch,
# also easier to recover in case of disk failure during SYNC
autosave 100
